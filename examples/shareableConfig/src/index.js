// const noDupTag = require('../../plugins/no-dup-tag/src');

module.exports = {
  extend: require('../../../packages/basic-config'),
  // plugins: [noDupTag],
  rules: {
    // 'no-dup-tag': ['on'],
  },
};
